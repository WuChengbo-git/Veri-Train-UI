# Veri-Train UI クイックスタートガイド

## 🚀 5分で起動

### 1. 環境変数の設定

```bash
# .envファイルを作成
cat > .env << 'EOF'
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000
EOF
```

### 2. 開発サーバーの起動

```bash
npm run dev
```

ブラウザで http://localhost:3000 を開く

### 3. 動作確認

以下のページが正常に表示されることを確認:
- ✅ Dashboard (ダッシュボード)
- ✅ Models (モデル管理)
- ✅ Datasets (データセット管理)
- ✅ Experiments (実験管理)
- ✅ Evaluation (評価)
- ✅ Reports (レポート)
- ✅ Settings (設定)

## 📦 本番ビルド

```bash
# ビルド
npm run build

# プレビュー
npm run preview
```

ビルド成果物は `dist/` に出力されます。

## 🔧 バックエンドAPI接続

### モックデータで動作確認(推奨)

バックエンドが未実装の場合、以下のMock Service Workerを使用できます:

```bash
# MSWのインストール
npm install -D msw

# MSWの初期化
npx msw init public/ --save
```

### 実際のAPIに接続

`.env`ファイルで正しいエンドポイントを設定:

```env
VITE_API_BASE_URL=https://your-api-domain.com/api/v1
VITE_WS_URL=wss://your-api-domain.com
```

## 🎯 主要機能の使い方

### モデル一覧の表示
1. 左サイドバーから「モデル」をクリック
2. フィルター機能で検索(タイプ、ステータス)
3. モデル名をクリックして詳細表示

### データセット生成
1. 「データセット」→「新規データセット生成」
2. ウィザードに従って設定
3. コスト見積もりを確認
4. 生成開始

### 実験の実行
1. 「実験」→「新規実験」
2. モデル、データセット、設定を選択
3. 実験開始
4. リアルタイム進捗監視

## 🐛 トラブルシューティング

### ポート3000が使用中
```bash
# vite.config.tsでポートを変更
server: {
  port: 3001,  // 別のポートを指定
}
```

### WebSocket接続エラー
- `.env`の`VITE_WS_URL`が正しいか確認
- バックエンドサーバーが起動しているか確認
- CORS設定を確認

### ビルドエラー
```bash
# node_modulesを削除して再インストール
rm -rf node_modules package-lock.json
npm install
```

## 📚 次のステップ

- [ARCHITECTURE.md](./ARCHITECTURE.md) - 技術アーキテクチャ詳細
- [PROJECT_SUMMARY.md](./PROJECT_SUMMARY.md) - プロジェクト総括
- [README.md](./README.md) - 完全なドキュメント

## 🤝 開発に参加

1. 詳細ページの実装から開始することを推奨
2. `src/pages/Models/ModelDetail.tsx`が良い出発点
3. 既存のパターンに従って実装

---

**注意**: 現在はフロントエンドのみ実装済み。バックエンドAPIは別途実装が必要です。
