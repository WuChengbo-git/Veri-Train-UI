import { Typography } from 'antd';

const { Title } = Typography;

const DatasetDetail = () => {
  return (
    <div>
      <Title level={2}>データセット詳細</Title>
      <p>このページは現在開発中です。</p>
    </div>
  );
};

export default DatasetDetail;
