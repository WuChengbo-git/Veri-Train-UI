import { Typography } from 'antd';

const { Title } = Typography;

const ModelDetail = () => {
  return (
    <div>
      <Title level={2}>ModelDetail</Title>
      <p>このページは現在開発中です。</p>
    </div>
  );
};

export default ModelDetail;
