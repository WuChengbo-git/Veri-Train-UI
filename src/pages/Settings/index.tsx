import { Typography } from 'antd';

const { Title } = Typography;

const Settings = () => {
  return (
    <div>
      <Title level={2}>設定</Title>
      <p>このページは現在開発中です。</p>
    </div>
  );
};

export default Settings;
