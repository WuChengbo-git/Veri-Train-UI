import { Typography } from 'antd';

const { Title } = Typography;

const GenerateDataset = () => {
  return (
    <div>
      <Title level={2}>GenerateDataset</Title>
      <p>このページは現在開発中です。</p>
    </div>
  );
};

export default GenerateDataset;
