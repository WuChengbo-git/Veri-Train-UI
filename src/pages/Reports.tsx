import { Typography } from 'antd';

const { Title } = Typography;

const Reports = () => {
  return (
    <div>
      <Title level={2}>Reports</Title>
      <p>このページは現在開発中です。</p>
    </div>
  );
};

export default Reports;
