import { Typography } from 'antd';

const { Title } = Typography;

const Evaluation = () => {
  return (
    <div>
      <Title level={2}>Evaluation</Title>
      <p>このページは現在開発中です。</p>
    </div>
  );
};

export default Evaluation;
